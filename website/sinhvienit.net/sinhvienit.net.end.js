$(document).ready(function () {
    var btnContinue = $('#btnSubmit1');

    if (undefined !== btnContinue && btnContinue.length > 0) {
        btnContinue.get(0).click();
    }

    
    var btnRedir = $('#btn-redir');
    if (undefined !== btnRedir && btnRedir.length > 0) {
        btnRedir.get(0).click();
    }
    
});
