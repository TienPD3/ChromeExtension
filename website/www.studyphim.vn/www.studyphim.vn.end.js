$(document).ready(function() {
    // var eButton = $('.navbar-right').find('li').find('button');
    // if (eButton.find('.fa-user').length > 0) {
    //     eButton.find('.fa-user').remove();
    //     var eHtml = '<span class="label label-warning">VIP</span>'
    //     eButton.prepend(eHtml);
    // }
    // eButton.find('.label-warning').after('Ti Ni');

//     var popup = document.getElementsByClassName("playable");
//     if(popup.length > 0){
// 		popup[0].remove();
// 	}
});